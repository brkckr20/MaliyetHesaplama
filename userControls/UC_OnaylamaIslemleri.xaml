<UserControl x:Class="MaliyeHesaplama.userControls.UC_OnaylamaIslemleri"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MaliyeHesaplama.userControls"
             mc:Ignorable="d" 
             xmlns:control="clr-namespace:FilterDataGrid;assembly=FilterDataGrid"
             d:DesignHeight="450" d:DesignWidth="800" FontFamily="Bahnschrift Light">
    <Grid>
        <TabControl x:Name="tabControlOnaylamaIslemleri" Margin="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="Orange"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TabControl.Resources>
            <TabItem Header="Genel" x:Name="sip" Loaded="sip_Loaded">
                <control:FilterDataGrid
                            x:Name="gridSip" FontSize="12" VerticalContentAlignment="Center" 
                            IsReadOnly="True" GridLinesVisibility="All" 
                            HorizontalGridLinesBrush="#ececec" VerticalGridLinesBrush="#ececec"
                            FilterLanguage="Turkish" ShowStatusBar="True" AutoGenerateColumns="True"
                            AutoGeneratingColumn="gridSip_AutoGeneratingColumn"
                            >
                    <control:FilterDataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Onay Durumunu Değiştir" x:Name="odd">
                                <MenuItem Header="Onayla" x:Name="onayla" Click="onayla_Click"/>
                                <MenuItem Header="Onayı Kaldır" x:Name="kaldir" Click="kaldir_Click"/>
                            </MenuItem>
                            <Separator/>
                            <MenuItem Header="Onaylananlar" x:Name="onayli" Click="onayli_Click"/>
                            <MenuItem Header="Bekleyenler" x:Name="bekleyen" Click="bekleyen_Click"/>
                        </ContextMenu>
                    </control:FilterDataGrid.ContextMenu>
                </control:FilterDataGrid>
            </TabItem>
        </TabControl>

    </Grid>
</UserControl>
