<UserControl x:Class="MaliyeHesaplama.userControls.UC_KumasKarti"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MaliyeHesaplama.userControls"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:common="clr-namespace:MaliyeHesaplama.userControls.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Name="KumasKarti">
    <UserControl.Resources>
        <!--<Style TargetType="syncfusion:GridHeaderCellControl" x:Key="headerStyle">
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>-->
    </UserControl.Resources>
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!--<common:ButtonBar Grid.Row="0" x:Name="ButtonBar" DockPanel.Dock="Top" />-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,2,0,0">
                <DockPanel>
                    <TextBlock VerticalAlignment="Center" Width="110" Text="Kumaş Kodu :"/>
                    <TextBox IsEnabled="False" x:Name="txtKodu" Width="124" HorizontalAlignment="Left" Height="24" VerticalContentAlignment="Center"/>
                    <Button Click="btnKumasKodu_Click" x:Name="btnKumasKodu" Content="🔎" Background="Transparent" Width="24" HorizontalAlignment="Left" BorderBrush="#ececec"/>
                    <TextBlock x:Name="lblKumasAdi" VerticalAlignment="Center" Margin="4,0"/>
                </DockPanel>
                <DockPanel Margin="0,2">
                    <TextBlock VerticalAlignment="Center" Width="110" Text="Dokuma Cinsi :"/>
                    <TextBox IsEnabled="False" x:Name="txtDokumaCinsi" Width="124" HorizontalAlignment="Left" Height="24" VerticalContentAlignment="Center"/>
                    <Button x:Name="btnDokumaCinsi" Click="btnDokumaCinsi_Click" Content="🔎" Background="Transparent" Width="24" HorizontalAlignment="Left" BorderBrush="#ececec"/>
                </DockPanel>
                <DockPanel>
                    <TextBlock VerticalAlignment="Center" Width="110" Text="Desen :"/>
                    <TextBox IsEnabled="False" x:Name="txtKumasDesen" Width="124" HorizontalAlignment="Left" Height="24" VerticalContentAlignment="Center"/>
                    <Button x:Name="btnDesen" Click="btnDesen_Click" Content="🔎" Background="Transparent" Width="24" HorizontalAlignment="Left" BorderBrush="#ececec"/>
                </DockPanel>
                <DockPanel HorizontalAlignment="Left" Margin="0,1,0,0">
                    <TextBlock VerticalAlignment="Center" Width="108" Text="İpliği Boyalı ?:"/>
                    <CheckBox x:Name="chckIpBoyali" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <CheckBox.LayoutTransform>
                            <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                        </CheckBox.LayoutTransform>
                    </CheckBox>
                </DockPanel>
                <DockPanel Margin="0,1,0,2">
                    <TextBlock VerticalAlignment="Center" Width="110" Text="Açıklama :"/>
                    <TextBox x:Name="txtAciklama" VerticalContentAlignment="Center" Width="auto" Height="24"/>
                </DockPanel>
                <DockPanel>
                    <TextBlock Text="Kullanımda :" VerticalAlignment="Center" Width="108"/>
                    <CheckBox x:Name="chckKullanimda">
                        <CheckBox.LayoutTransform>
                            <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                        </CheckBox.LayoutTransform>
                    </CheckBox>
                </DockPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
