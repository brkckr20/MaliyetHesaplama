<Window x:Class="MaliyeHesaplama.v2.Windows.winStokSecimiV2"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         Title="Stok Seçimi" Height="500" Width="900" WindowStartupLocation="CenterScreen">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" x:Name="grid" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" SelectionMode="Extended"
                  GridLinesVisibility="All" HorizontalGridLinesBrush="LightGray" VerticalGridLinesBrush="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kodu" Binding="{Binding InventoryCode}" Width="100"/>
                <DataGridTextColumn Header="Adı" Binding="{Binding InventoryName}" Width="200"/>
                <DataGridTextColumn Header="Miktar" Binding="{Binding Quantity, StringFormat={}{0:N2}}" Width="100"/>
                <DataGridTextColumn Header="Birim" Binding="{Binding Unit}" Width="80"/>
                <DataGridTextColumn Header="Birim Fiyat" Binding="{Binding UnitPrice, StringFormat={}{0:N2}}" Width="100"/>
                <DataGridTextColumn Header="KDV (%)" Binding="{Binding Vat}" Width="80"/>
                <DataGridTextColumn Header="Depo Id" Binding="{Binding WarehouseId}" Width="60" Visibility="Hidden"/>
                <DataGridTextColumn Header="Malzeme Id" Binding="{Binding InventoryId}" Width="60" Visibility="Hidden"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="btnAktar" Content="Aktar" Width="80" Height="30" Margin="5,0,5,0" Click="btnAktar_Click"/>
            <Button x:Name="btnVazgec" Content="Vazgeç" Width="80" Height="30" Margin="5,0,0,0" Click="btnVazgec_Click"/>
        </StackPanel>
    </Grid>
</Window>